# Быстрый старт - Публикация на GitHub

## ⚠️ ВАЖНО - Проверка безопасности

Перед публикацией убедитесь:

```bash
# Проверьте, что .env НЕ будет загружен
cat .gitignore | grep ".env"
```

Должно быть: `.env` в списке игнорируемых файлов ✅

## 🚀 Быстрая публикация (5 шагов)

### 1. Инициализация Git

```bash
cd /Users/nick/Sites/localhost
git init
git add .
git commit -m "Initial commit: Laravel forum application"
```

### 2. Создайте репозиторий на GitHub

- Перейдите на https://github.com/new
- Название: например `laravel-forum`
- Описание: `Laravel-based forum application`
- Выберите Public или Private
- **НЕ** добавляйте README/LICENSE/.gitignore
- Нажмите "Create repository"

### 3. Свяжите с GitHub

```bash
# Замените YOUR_USERNAME и YOUR_REPO на ваши данные
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

### 4. Проверка безопасности

После публикации зайдите на GitHub и проверьте:
- ❌ Файл `.env` НЕ должен быть виден
- ✅ Файл `.env.example` должен быть виден
- ✅ README.md отображается правильно

### 5. Готово!

Ваш проект опубликован! 🎉

## 📋 Инструкция для других разработчиков

Добавьте в описание репозитория на GitHub:

```
## Установка

1. git clone https://github.com/YOUR_USERNAME/YOUR_REPO.git
2. cd YOUR_REPO
3. composer install
4. npm install
5. cp .env.example .env
6. Настройте .env (база данных, ключи API)
7. php artisan key:generate
8. php artisan migrate
9. php artisan storage:link
10. npm run build
11. php artisan serve
```

## 🔒 Настройка .env после клонирования

Другие разработчики должны:

1. Скопировать `.env.example` в `.env`
2. Настроить базу данных:
   ```
   DB_DATABASE=your_database
   DB_USERNAME=your_username
   DB_PASSWORD=your_password
   ```
3. Получить ключи:
   - hCaptcha: https://www.hcaptcha.com/
   - StopForumSpam: https://www.stopforumspam.com/keys
4. Сгенерировать APP_KEY:
   ```bash
   php artisan key:generate
   ```

## ⚡ Быстрая проверка перед коммитом

```bash
# Убедитесь, что .env не будет закоммичен
git status | grep .env

# Если команда ничего не вывела - всё хорошо! ✅
# Если показала .env - ОСТАНОВИТЕСЬ! ❌
```

## 🆘 Если случайно загрузили .env

1. **Немедленно смените все секретные ключи!**
2. Удалите файл из Git:
   ```bash
   git rm --cached .env
   git commit -m "Remove .env from tracking"
   git push
   ```
3. Сгенерируйте новые ключи:
   ```bash
   php artisan key:generate
   ```

## 📚 Подробная инструкция

Смотрите файл `PUBLISHING_TO_GITHUB.md` для детальных инструкций.

---

**Важно**: `.env` содержит секретные данные и НЕ должен быть в репозитории!
